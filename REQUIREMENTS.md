# 智能出题与判分 Agent 应用 — 需求文档

## 1. 产品概述

面向家长的智能学习助手：支持两种出题方式（**按教材选题**、**对话出题**），生成可打印 PDF；孩子完成练习后，由家长拍照上传，Agent 智能判分并记录成绩。

**产品形态**：移动端 Web（手机浏览器访问）；界面按手机触控设计，不单独做桌面端。

## 2. 两种出题方式（需清晰区分）

产品提供 **两条入口、一套下游流程**。出题方式在首页即可区分选择；生成题目并确认后，统一进入「PDF → 线下作答 → 拍照判分 → 成绩」闭环。


| 对比项  | 方式 A：按教材选题                             | 方式 B：对话出题                          |
| ---- | -------------------------------------- | ---------------------------------- |
| 入口文案 | 按教材出题                                  | 对话出题                               |
| 家长操作 | 选学段 / 年级 / 科目 / 版本 → 选单元或学期范围 → 配置题量难度 | 用自然语言描述需求，可多轮补充                    |
| 出题依据 | 预置教材 PDF 知识库（RAG，紧扣课本单元）               | 家长对话意图（知识点 / 题型 / 数量等）；可选关联年级科目作约束 |
| 典型例子 | 「小学三年级数学人教版 · 第二单元」                    | 「出 10 道两位数加减法」「出一些第三人称的英语选择题」      |
| 适用场景 | 跟进度复习、单元检测、半学期/整学期组卷                   | 针对薄弱点、临时加练、不限定某单元                  |
| 下游流程 | 相同：确认题目 → 生成 PDF → 拍照判分 → 记成绩          | 相同                                 |


> **设计原则**：首页用两个并列入口明确区分，避免混在同一个「开始出题」里；练习记录中标注本次来源（教材 / 对话），便于家长回顾。



### 2.1 方式 A — 按教材选题

- 学段：小学 / 初中
- 年级：一年级～九年级（或按学段细分）
- 科目：语文、数学、英语等（可按教材体系扩展）
- 教材版本：支持主流版本（如人教版、苏教版等，具体版本一期可配置）
- 出题范围：
  - **按单元**：选择某一单元或若干单元
  - **半学期**：覆盖半学期所学内容
  - **整学期**：覆盖整学期所学内容
- 可配置项：题目数量、难度、题型比例、是否同时生成答案卷
- Agent 依据所选课本内容与知识点（RAG）智能生成练习题



### 2.2 方式 B — 对话出题

- 家长进入对话页，用自然语言说明出题需求，例如：
  - 「出 10 道关于两位数加减的题目」
  - 「出一些第三人称的英语选择题」
  - 「给我 15 道三年级口算，难度简单一点」
- 支持多轮对话：Agent 可追问缺失信息（科目、题量、题型、难度等），家长可补充或修改
- Agent 解析意图后生成题目列表；家长确认（可要求「再出几道」「改成选择题」）后再生成 PDF
- 一期可不强制绑定某本教材；若家长提到年级/科目，作为生成约束写入练习元数据
- 对话记录可保留在当次练习会话中，便于回溯「为什么这样出题」



### 2.3 共用能力（两种方式汇合后）

以下能力与出题入口无关，流程一致：

**题型（一期可先支持子集）**

- 选择题、填空题、判断题、简答 / 解答题、计算题（数学）

**可打印 PDF**

- 导出适合 A4 打印的练习卷；可选答案与解析卷
- 标题体现来源：教材范围 或 对话主题摘要；含日期、孩子信息栏、作答区、页码

**拍照上传与智能判分**

- 家长拍照上传 → 视觉模型识别 → 对照答案判分
- 输出每题对错、总分、得分率；可选错题解析
- 图片模糊 / 缺页时提示重拍

**成绩记录与学习反馈**

- 记录时间、出题方式（教材 / 对话）、范围或对话摘要、题量、得分、错题
- 支持按科目等查看历史；错题本 / 学习报告为可选增强



## 3. 典型使用流程



### 3.1 方式 A：按教材选题

```
首页 → 按教材出题
        ↓
选择学段/年级/科目/教材版本
        ↓
选择出题范围（单元 / 半学期 / 整学期）
        ↓
配置题量、难度、题型（可选）
        ↓
Agent 按教材 RAG 出题 → 家长确认
        ↓
        └──→（汇合）生成 PDF → … 见 3.3
```



### 3.2 方式 B：对话出题

```
首页 → 对话出题
        ↓
自然语言描述需求（可多轮补充）
        ↓
Agent 解析意图并出题 → 家长确认（可继续对话调整）
        ↓
        └──→（汇合）生成 PDF → … 见 3.3
```



### 3.3 共用下游流程

```
生成并下载/打印 PDF
        ↓
孩子线下作答
        ↓
家长拍照上传答卷
        ↓
智能判分 → 家长确认 → 写入成绩记录
        ↓
家长查看得分、错题与历史成绩
```



## 4. 功能清单（按优先级）



### P0 — 一期必做

- [ ] 首页区分「按教材出题」「对话出题」两个入口
- [ ] 方式 A：学段、年级、科目、单元 / 半学期 / 整学期选择与教材出题
- [ ] 方式 B：对话出题（意图理解 + 生成题目 + 多轮补充）
- [ ] 出题后确认，再生成练习卷 PDF
- [ ] 答卷拍照上传与智能判分
- [ ] 成绩记录（含出题方式标记）与历史查询



### P1 — 二期增强

- [ ] 多教材版本管理
- [ ] 答案卷 / 解析卷 PDF
- [ ] 对话出题可选「参考某教材/单元」作约束
- [ ] 错题本与错题重练
- [ ] 难度与题型精细配置
- [ ] 学习报告 / 薄弱点分析
- [ ] 多孩子档案管理



### P2 — 后续扩展

- [ ] 多孩子账号 / 家庭账号
- [ ] 手写体识别优化与多页答卷拼接



## 5. 非功能需求


| 类别   | 要求                                         |
| ---- | ------------------------------------------ |
| 可用性  | 两种出题方式入口清晰；手机可完成「出题 → 下载/打印 PDF → 拍照 → 看分」 |
| 移动体验 | 按手机屏幕与触控设计；对话页适合单手输入；支持拍照上传                |
| 准确性  | 教材模式紧扣单元知识点；对话模式贴合家长表述的知识点与题型；客观题判分高准确     |
| 性能   | 出题、对话回复与 PDF 生成有进度反馈；判分有进度反馈               |
| 隐私   | 答卷图片、对话与成绩数据加密存储，明确用途与保留策略                 |
| 打印体验 | PDF 在常见家用打印机上排版正常、墨量友好                     |




## 6. 教材内容来源

- **来源方式**：由产品方提前准备好相关教材的 **PDF 文件**（按学段 / 年级 / 科目 / 版本整理），放在本地 `book/`，不提交到 git
- **入库方式**：一次处理一本。入口在 `apps/api/ingest/`（离线命令，不是 HTTP 服务，也不写在出题图 `agents/textbook/` 里）。输入为 PDF 路径，以及学段、年级、科目、版本、学期。流程为：解析 PDF → 按单元切开 → 用百炼 `qwen3.7-text-embedding` 计算 embedding → 写入 PostgreSQL + pgvector。检索时先按这五项元数据过滤，再按单元或向量相似度取块，供 **方式 A** 出题引用
- **切分**：以教材单元（或目录中的同级结构）为界，每块记录单元名和页码。识别不到单元时入库失败，不写入无单元标记的整本
- **元数据**：每份教材 PDF 需标注学段、年级、科目、版本、学期等；这些字段由命令参数传入，不从某一册文件名写死
- **与方式 B 的关系**：对话出题一期可不依赖教材库；二期可支持「对话 + 指定教材范围」增强相关性
- **更新**：同一本（五项元数据相同）再次执行时，先删除该书已有块再写入，避免重复。更换其他教材时用同一命令，只改 PDF 路径和元数据



## 7. 技术依赖与能力

- **教材知识库**：预置教材 PDF 结构化内容 + RAG（主要服务方式 A）
- **大模型 / Agent（双实现，按场景选用）**：
  - **方式 A（按教材）**：**LangGraph** 固定工作流（选题 → RAG 出题 → 确认 → PDF）
  - **方式 B（对话）**：**DeepAgents** 多轮理解与委派出题 → 确认 → PDF
  - **下游共用**：判分等可复用同一套 Tool / 子图，由 FastAPI 在汇合后调用
- **多模态能力**：答卷图片 OCR / 视觉理解与判分
- **PDF 引擎**：教材解析；练习卷中文排版与公式渲染导出
- **API 与 Agent 集成**：FastAPI 按出题方式路由到对应实现，通过 `ainvoke` / `astream` / HITL `resume` 调用，用 `thread_id` 串联同一次练习会话



## 8. 技术栈


| 层级         | 技术选型                                          | 用途                                 |
| ---------- | --------------------------------------------- | ---------------------------------- |
| 前端         | Vite + React + TypeScript（SPA）                | 家长端移动 Web：双入口出题、对话、上传、成绩；不强制 Next.js |
| UI         | Tailwind CSS（移动优先）                            | 手机端界面样式                            |
| 路由         | React Router                                  | 客户端路由；与 FastAPI 职责分离               |
| 后端 / API   | Python FastAPI                                | 业务 API；按方式路由调用 Agent；流式 / 续跑；文件上传  |
| 方式 A Agent | **LangGraph**（StateGraph 固定流）                 | 按教材选题出题：节点与边确定性编排 + interrupt 确认   |
| 方式 B Agent | **DeepAgents**（基于 LangGraph）                  | 对话出题：主 Agent + 子 Agent / 工具，多轮意图理解 |
| 运行时        | LangGraph Checkpointer（上线落 Postgres）        | 两套实现共用会话持久化与 HITL 中断恢复；禁止生产用纯内存 |
| 大模型        | 阿里云百炼（通义）：出题 `qwen3.7-plus`；向量 `qwen3.7-text-embedding`；判分可用百炼多模态 | 出题、对话、解析；教材 embedding；答卷视觉判分 |
| 教材 PDF 解析  | LlamaParse / Unstructured / pdfplumber        | 提取教材文本与结构，按单元切分                    |
| 向量检索       | PostgreSQL + pgvector（或 Chroma）               | 教材 RAG（方式 A）                       |
| 练习卷 PDF 生成 | WeasyPrint / ReportLab（或 Playwright HTML→PDF） | 生成可打印 A4 练习卷 / 答案卷（共用 Tool）        |
| 数据库        | PostgreSQL + SQLAlchemy / SQLModel            | 用户、练习（含出题方式）、成绩、对话会话               |
| 对象存储       | 本地存储（开发）/ S3 兼容（上线）                           | 教材 PDF、答卷图片、生成的练习 PDF              |
| 认证         | FastAPI JWT / OAuth2                          | 家长登录与会话；前端直连 API，不引入 NextAuth     |
| 部署（一期）   | Docker Compose：静态前端（Nginx）+ FastAPI（内嵌 Agent）+ PostgreSQL(+pgvector) + 对象存储 / 云主机 | 同机多容器；Agent 不单独部署；前端无 Node 常驻进程 |




### 8.1 Agent 架构（按场景分工）

```
FastAPI（按入口路由）
  │
  ├─ 方式 A：按教材出题 ──→ LangGraph 固定流
  │     选题/范围/配置 → RAG 出题 → HITL 确认 → PDF Tool
  │
  ├─ 方式 B：对话出题 ────→ DeepAgents
  │     多轮对话 → 出题 → HITL 确认 → PDF Tool
  │
  └─ 下游共用（两种方式汇合后）
        拍照上传 → 判分（Vision Tool / 子图）→ HITL 确认成绩 → 写库
```

- **方式 A 用 LangGraph**：步骤固定、要可控、可逐步调试；用节点表达「检索 → 出题 → 等确认 → 出 PDF」，少依赖模型自主规划
- **方式 B 用 DeepAgents**：开放对话、多轮补充，适合 harness + 子 Agent / 工具委派
- **下游共用**：PDF、判分、落库做成共用模块，避免两套重复实现
- **状态分工**：Checkpointer 保存对话与断点；业务表保存练习、PDF 路径、成绩及出题方式标记
- **学习收益**：同一产品内对比「固定图」与「DeepAgents harness」，便于掌握两层抽象（非为维护两套完整产品）



### 8.2 技术栈与业务流程对应

```
手机 Web → FastAPI
              ├─ /textbook → LangGraph（方式 A）─┐
              └─ /chat     → DeepAgents（方式 B）─┴─→ HITL 确认 → PDF
                                                          ↓
                                    打印作答 → 上传 → 判分 → HITL 确认 → 成绩入库
```



### 8.3 部署方式（一期）

**原则**：

- Agent（LangGraph / DeepAgents）作为 FastAPI 进程内库调用，**不单独部署** Agent 服务
- 前端为一期 **Vite 构建的 SPA 静态资源**，由 Nginx / Caddy 托管；**不强制 Next.js**（家长工具无强 SEO / SSR 需求，鉴权与流式均由 FastAPI 承担）

**一期推荐拓扑（Docker Compose / 云主机）**

```
[ 浏览器 / 手机 Web ]
        │
        ▼
   反向代理（HTTPS）
        │
   ┌────┴────┐
   ▼         ▼
 静态前端    FastAPI（内嵌 LangGraph / DeepAgents）
 (Vite build │
  + Nginx)   │
     ┌───────┼────────┐
     ▼       ▼        ▼
 PostgreSQL 对象存储   外部 LLM API
 (+pgvector) (S3 兼容) (阿里云百炼 / DashScope)
```

| 组件 | 一期做法 | 说明 |
| ---- | -------- | ---- |
| 前端 | Vite 构建静态文件，Nginx 托管 | SPA；无 Node 常驻进程；`/api` 反代到 FastAPI |
| 后端 | FastAPI 独立容器 | 路由调用 Agent；流式 / HITL `resume`；JWT 鉴权 |
| Agent | **同进程嵌入 FastAPI** | `ainvoke` / `astream` / `resume`；不另起服务 |
| 数据库 | PostgreSQL + pgvector | 业务表 + 教材向量；Checkpointer 落库 |
| 对象存储 | 开发用本地盘；上线用 S3 兼容 | 教材 PDF、答卷图、生成练习卷 |
| 反向代理 | Nginx / Caddy 等 | HTTPS、上传体积、流式与长超时；SPA `index.html` fallback |

**必须满足的运行约束**

- **Checkpointer 持久化**：上线使用 Postgres（或等价）Checkpointer；禁止仅用内存实现，否则进程重启后 HITL 续跑失败
- **长请求与流式**：出题 / 判分 / PDF 可能数十秒级；网关与 uvicorn 超时需放宽，对话与生成走 SSE / 流式接口
- **PDF 镜像依赖**：WeasyPrint / Playwright 等需在 Docker 镜像中预装系统库，勿用「纯 Python 精简镜像」硬上
- **密钥与限流**：百炼密钥 `bailian_api_key`、模型名（如 `QUIZ_MODEL` / `EMBEDDING_MODEL`）与对象存储凭证仅通过环境变量 / 仓库根目录 `.env` 注入（`.env` 不进 git）；对模型调用做基础限流与错误重试

**环境区分**

| 环境 | 存储 | Checkpointer | 对象文件 |
| ---- | ---- | ----------- | -------- |
| 本地开发 | 可 Docker 起 Postgres，或临时简化 | 可用内存（仅本地） | 本地目录 |
| 一期上线 | 托管或同机 Postgres(+pgvector) | **必须** Postgres Checkpointer | S3 兼容 |

**二期再考虑（一期不做）**

- 将 PDF 生成、答卷判分拆到独立 worker / 任务队列（Celery、Arq 等），避免长任务占满 API worker
- 独立 Agent 运行时（如 LangGraph Platform）——仅在需要多服务扩缩、强隔离时再评估
- 前端与 API 分域、多副本水平扩展
- 若出现强 SEO 落地页或需 SSR，再评估引入 Next.js（应用本体仍可保持 SPA）



## 9. 待确认事项

1. 一期优先覆盖哪些科目与教材版本？（对应需准备哪些 PDF）
2. 账号体系：游客可用，还是必须登录？是否支持多孩子？
3. 主观题（作文、简答）判分深度：只给参考分，还是详细评语？
4. 练习卷 PDF 是否必须支持数学公式、图形题等复杂排版？
5. 大模型配额与预算（阿里云百炼：出题 / 向量 / 判分调用量）
6. 对话出题是否一期就要强制选择年级/科目，还是完全自由描述？

