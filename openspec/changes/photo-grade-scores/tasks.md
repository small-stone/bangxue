# Tasks

## 1. Backend storage and grading API

- [x] 1.1 新增答卷上传目录与成绩 JSON 存储模块（按邮箱分桶、attempt 含逐题对错），并写清 `data/` 不进 git。验证：单元或手工调用能写入/读回一条 attempt
- [x] 1.2 实现 `agents/shared` 判分函数：对照题目答案输出每题对错；有 `bailian_api_key` 走多模态，否则确定性演示回退并标记 `demo`。验证：无 key 时返回稳定假结果；有 key 时可对样例图调用不崩溃
- [x] 1.3 增加 FastAPI 路由：上传并判分、获取 attempt、确认写入成绩、按邮箱列出成绩与错题聚合；确认接口无邮箱时拒绝落库。验证：用 curl/httpie 走通「判分 → 确认 → 列表」；游客确认返回明确错误
- [x] 1.4 在 `apps/api/README.md` 说明上传路径、成绩文件、`X-Parent-Email`、判分超时与演示回退。验证：README 含上述四项

## 2. Upload and grading result UI

- [x] 2.1 新增 `/grade/upload` 页（对齐 mockup-05）：上下文标签、拍照/相册、多页缩略图、无照片不可点「开始判分」。验证：手机或开发者工具可选图，无图时按钮禁用或提示
- [x] 2.2 接通首页「去上传」与 PDF 结果页「去拍照判分」入口，带上当前 quiz。验证：从首页/结果页能进入上传页且上下文正确；quiz 失效时有提示
- [x] 2.3 新增 `/grade/result/:attemptId`（对齐 mockup-06）：得分环、逐题对错、判分中/失败态、确认成绩、「查看错题」「成绩记录」。验证：演示判分后确认，已登录账号成绩列表增加一条
- [x] 2.4 新增 `/grade/wrong/:attemptId` 单次错题页；满分时说明无错题。验证：有错题显示列表；全对显示空态文案

## 3. Score records and wrong-question book

- [x] 3.1 改写 `Scores`：去掉硬编码演示数据，拉取已登录成绩；汇总、科目筛选、空态、点击进详情/错题。验证：登录后确认的成绩出现；按科目筛选正确；未登录有登录引导且无串号数据
- [x] 3.2 新增 `/me/wrong-book`，「我的 · 错题本」跳转：登录展示聚合错题，游客引导登录，无错题空态。验证：三种态均可观察，且不做重练入口

## 4. End-to-end slice

- [x] 4.1 走通：出题 → PDF → 上传 → 判分 → 确认 → 成绩页 → 错题本。验证：已登录家长完整路径无断链；游客可看结果但成绩库不增加
