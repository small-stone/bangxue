# Tasks

## 1. 需求与仓库约定

- [x] 1.1 更新 `REQUIREMENTS.md` 第 6 节：一次入库一本；输入为 PDF 路径和学段、年级、科目、版本、学期；按单元切分后写入 pgvector；同一本再次执行则替换；代码位于 `apps/api/ingest/`，出题图只检索不拆分。验证：文档中能找到该入口、目录和替换语义
- [x] 1.2 将 `book/` 加入根 `.gitignore`，并把已跟踪的教材 PDF 从 git 索引移除且保留工作区文件。验证：`git status` 不列出 `book/` 下的 PDF，文件仍在磁盘上

## 2. 入库包

- [x] 2.1 创建 `apps/api/ingest/`（解析、按单元拆分、写入、命令入口），不在 `agents/textbook/` 中实现拆分，也不注册 HTTP 路由。验证：目录存在，且 `agents/textbook/` 无 PDF 解析代码
- [x] 2.2 用 pdfplumber 抽取带页码的文本，并按单元标题切开；识别不到单元时命令失败且不写库。验证：对一份无单元标题的样例文本，函数报错且数据库行数不变
- [x] 2.3 按五元组删除该书旧块再写入新块与 embedding；embedding 模型与密钥来自环境变量。验证：同一输入连续写入两次后，该书块数等于第二次的块数，另一五元组的行仍在
- [x] 2.4 命令接收 `--pdf` 与五项元数据，不把某一册路径写死。验证：`--help` 列出这些参数，源码中不出现一年级上册文件名常量

## 3. 用第一本验收

- [x] 3.1 对人教版数学一年级上册执行入库命令。验证：库中存在该五元组的块，且每块有单元名和页码
- [x] 3.2 按该册元数据加一个实际单元名检索。验证：返回正文属于该单元，不包含其他年级册次
