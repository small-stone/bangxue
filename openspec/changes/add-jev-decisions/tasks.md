# Tasks

## 1. 需求与规划上下文

- [ ] 1.1 更新 `REQUIREMENTS.md`：判分改为「视觉识别 → Jev 对错/置信度/重拍」；方式 B 增加 Jev 判断信息是否足够并追问；方式 A 在确认前由 Jev 检查是否越出教材范围；技术栈增加 Jev，并写明评语与出题正文仍用生成式模型、成绩仍须家长确认。验证：文档中能找到上述三条，且不再把对错判断写成仅由 Vision 模型完成
- [ ] 1.2 在 `openspec/config.yaml` 的 context 中注明 Jev 只做封闭判断、不生成、不看图。验证：后续 `openspec instructions` 能读到该约束

## 2. Jev 客户端（apps/api）

- [ ] 2.1 在 `agents/shared` 增加 Jev 客户端：从环境变量读 API key，提交预定义问题，返回选择与置信度；缺少 key 或请求失败时返回明确错误，不用随机结果冒充。验证：用假 HTTP 或官方示例请求，成功与失败各有一条测试
- [ ] 2.2 实现判分判断函数：输入识别作答与标准答案则返回对错与置信度；无识别作答则不调用 Jev 并标为未能识别；根据图像质量信号返回是否重拍。验证：单元测试覆盖 `grading-judgment` 的三个场景分支（可判、未能识别、应重拍）
- [ ] 2.3 实现流程判断函数：对话状态返回是否足够及所缺项枚举；题目与所选范围返回是否越界。信息不足或越界时不得返回「可以静默生成/静默当范围内题」。验证：单元测试覆盖 `flow-judgment` 的不足追问、足够出题、越界标出、范围内通过
- [ ] 2.4 低置信度只附加「需留意」标记，函数结果中不包含「跳过家长确认」。验证：低置信度用例的返回值仍要求确认

## 3. 接入点（不实现完整出题与 RAG）

- [ ] 3.1 `agents/shared` 的判分占位改为调用判分判断函数；`agents/chat`、`agents/textbook` 的占位改为在出题前/确认前调用流程判断函数。验证：从 `app` 导入这三处时能走到对应函数，且不出现对 Jev 的第二套 HTTP 封装
- [ ] 3.2 在 `apps/api` README 写明环境变量名、Jev 不单独部署、失败时判分标为待核对、流程判断不得静默放行。验证：README 含 key 名称与失败策略

## 4. 自检

- [ ] 4.1 对照两份 delta spec 核对：Jev 不读图、不写题目正文、不写主观评语。验证：测试或代码路径中不存在把图片字节或题目生成 prompt 发给 Jev 的调用
