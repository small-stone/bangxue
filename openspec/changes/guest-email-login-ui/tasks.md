# Tasks

## 1. Session and fixed code

- [x] 1.1 将登录校验改为仅接受验证码 `0000`；「发送验证码」只校验邮箱并提示演示码，不自动填入、不发网络请求。验证：错误码无法登录，`0000` 可登录
- [x] 1.2 清理/兼容旧 `phone` 会话字段，确保 `getSession` 只认 `email`。验证：旧 phone 会话视为未登录或可安全忽略

## 2. Login and Me UI

- [x] 2.1 打磨 `Login.tsx` 文案与布局对齐 `mockup-09-login.png`（邮箱、验证码、登录、游客继续）。验证：对照 mockup 一眼无手机号字段
- [x] 2.2 打磨 `Me.tsx` 未登录/已登录态对齐 `mockup-08-me.png`（头像区、菜单、登录或退出）。验证：未登录可进登录页；登录后显示脱敏邮箱并可退出

## 3. Guest mode badge

- [x] 3.1 增加共用「游客模式」标识组件，并在首页、出题相关页、成绩页、我的（未登录）展示；登录后隐藏。验证：清会话后上述页面可见标识，登录后消失
- [x] 3.2 确认无路由守卫拦截未登录访问主流程。验证：未登录可打开 `/`、`/textbook`、`/chat`、`/scores`、`/me`

## 4. Smoke check

- [x] 4.1 手动走通：游客逛首页 → 登录页游客继续 → 邮箱+`0000` 登录 → 我的显示邮箱 → 退出回游客。验证：全路径无控制台错误且行为符合 spec
