# Tasks

## 1. 一年级出题接口

- [x] 1.1 增加列出已入库单元的接口，只查询小学 / 一年级 / 数学 / 人教版 / 上册。验证：对该册返回库中的单元名；换年级则拒绝或返回空并带明确错误
- [x] 1.2 在 `agents/textbook` 中根据单元文本、题量、难度和题型比例调用环境变量中的生成式模型，返回题干；未要求答案时不含答案。验证：用假模型或录制响应，10/15/20/30 的数量一致，且关闭答案时无答案字段
- [x] 1.3 `POST /api/quizzes` 在写库前确认单元块存在，否则返回错误且不调用模型。验证：不存在的单元名得到 4xx，且不会返回题目
- [x] 1.4 用同一次题目生成练习 PDF；打开答案卷时再生成答案 PDF。验证：下载文件中的题干与 JSON 题干一致

## 2. 五屏界面

- [x] 2.1 按墨金稿实现首页（双入口、底部导航），不出现登录。验证：打开 `/` 可见「按教材出题」和「对话出题」；点击对话入口只显示尚未开放
- [x] 2.2 实现选题、范围、设置三屏，布局对齐 01–03 稿；仅小学一年级数学人教版可下一步，单元 chip 来自接口。验证：选三年级时下一步不可用；选一年级数学时能看到库里的单元
- [ ] 2.3 实现结果页：展示接口返回的题目、所选范围标题，以及 PDF 下载；答案仅在开关打开时出现。验证：从设置提交后，页面上的题干与接口 JSON 一致
- [x] 2.4 出题请求进行中禁用按钮并显示生成中；接口报错时展示错误文案而不是示例题。验证：断掉模型密钥或单元不存在时，结果页不出现稿面里的「23 + 45」

## 3. 串起来验收

- [ ] 3.1 从首页走到结果：一年级、数学、人教版、一个真实单元、题量 10。验证：结果页列出 10 道与该单元相关的题，并能下载练习 PDF
